<div class="row justify-content-center">
  <div class="col-md-8">
    <div class="card shadow-sm">
      <div class="card-body">
        <h5 class="card-title mb-3">Word → PDF</h5>
        <% if (typeof error !== 'undefined' && error) { %>
          <div class="alert alert-danger"><%= error %></div>
        <% } %>
        <form method="post" action="/pdf/word-to-pdf" enctype="multipart/form-data">
          <div class="mb-3">
            <label class="form-label">Upload .docx File</label>
            <input type="file" class="form-control" name="doc" accept=".docx" required>
          </div>
          <button class="btn btn-gradient">Convert</button>
        </form>
      </div>
    </div>
  </div>
</div>


